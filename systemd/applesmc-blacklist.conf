# On kernel 7.1+ the T2 SMC is driven by macsmc, not applesmc. applesmc still
# autoloads but cannot reach a T2 SMC and slows boot, so blacklist it. macsmc
# autoloads independently via ACPI and is unaffected.
blacklist applesmc
